[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Liveness seulement — /health/ready renvoie 503 si une variable manque (normal avant config complète)
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
